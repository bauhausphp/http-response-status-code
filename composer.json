{
  "name": "bauhaus/http-response-status",
  "description": "HTTP response status",

  "type": "library",
  "license": "MIT",

  "keywords": ["bauhaus", "http", "response", "response status", "status code"],

  "authors": [
    {
      "name": "Felipe Martins",
      "homepage": "https://blog.fefas.net"
    }
  ],

  "support": {
    "issues": "https://github.com/bauhausphp/http-response-status/issues",
    "source": "https://github.com/bauhausphp/http-response-status"
  },

  "scripts": {
    "unit-tests": "vendor/bin/phpunit -c tests/config/phpunit.xml --colors=always",
    "cs-tests": "vendor/bin/phpcs --standard=tests/config/phpcs.xml --colors -ps",
    "all-tests": [
      "@unit-tests",
      "@cs-tests"
    ]
  },

  "require": {
    "php": ">=7.1"
  },

  "autoload": {
    "psr-4": {
      "Bauhaus\\Http\\Response\\": "src/"
    }
  },

  "require-dev": {
    "phpunit/phpunit": "^6",
    "squizlabs/php_codesniffer": "^2.8"
  },

  "autoload-dev": {
    "psr-4": {
      "Bauhaus\\Http\\Response\\": [
        "tests/unit/"
      ]
    }
  }
}
